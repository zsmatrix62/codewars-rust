mod duplicate_encoder;
mod dubstep;
mod enough_is_enough;
mod fold_an_array;
mod build_a_pile_of_cubes;
mod k_primes;
mod going_to_zero_or_to_infinity;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
