mod duplicate_encoder;
mod dubstep;
mod enough_is_enough;
mod fold_an_array;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
